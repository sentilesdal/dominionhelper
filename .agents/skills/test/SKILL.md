# Test

Run the Vitest test suite.

## Commands

```bash
npm test          # Run the Vitest unit suite once
npm run test:watch # Watch mode
```

Vitest unit tests live in `tests/unit/` and use ES module imports from `src/`.
Playwright specs live in `tests/e2e/`.
